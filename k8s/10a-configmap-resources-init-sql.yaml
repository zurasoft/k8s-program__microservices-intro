apiVersion: v1
kind: ConfigMap
metadata:
  name: resources-init-sql
  namespace: k8s-program
data:
  resources.sql: |
    CREATE TABLE IF NOT EXISTS resources
    (
        id   INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        blob BYTEA
    );
